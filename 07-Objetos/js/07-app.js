// Nota (problema)

// Los Objetos (propiedaes) si se pueden reasignar aún con const o eliminar

const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 30,
    disponible: true
}

producto.disponible = false;

console.log(producto);

// 08 como prevenir on Object Methods
