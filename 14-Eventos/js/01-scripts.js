// Detectar que el HTML esté listo

document.addEventListener('DOMContentLoaded', () => {
    console.log('Documento listo');
});
// Es un evento que se ejeca una vez que es descargado todo el HTML



