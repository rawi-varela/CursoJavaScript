// Agregar nuevos valores a un Array

const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo'];

meses[0] = 'Nuevo mes';
meses[10] = 'Último mes'; 

console.table(meses);

// Objetos y Arreglos si se pueden modificar a pesar de const (a menos que se sellen los objetos)


